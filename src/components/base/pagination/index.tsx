import type { PaginationProps } from 'antd';
import { Pagination } from 'antd';

export type ComponentProps = PaginationProps;

const FunctionComponent = Pagination;
export default FunctionComponent;
