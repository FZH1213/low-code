import type { CheckboxProps, CheckboxOptionType } from 'antd';
import { Checkbox } from 'antd';

export type ComponentProps = CheckboxProps;
export type ComponentOptionProps = CheckboxOptionType;

const FunctionComponent = Checkbox;

export default FunctionComponent;
