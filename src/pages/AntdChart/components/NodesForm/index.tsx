import { Card, Form } from '@/components/base';
import React, { useEffect, useRef, useState } from 'react';
import styles from './index.less';

const NodesForm = () => {
    return (
        <Card className={styles.node_from} title="流程" size="small">
            <Form>

            </Form>
        </Card>
    )
}

export default NodesForm;